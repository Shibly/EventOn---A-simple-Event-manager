{% extends 'EventBundle::layout.html.twig' %}
{% block body %}
    <section class="events">
        <header id="menu">
            <a class="myButton" href="{{ path('user_register') }}">Register</a>
            <a class="myButton" href="{{ path('event_new') }}">Create new event</a>
            {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
                <a class="myButton" href="{{ path('logout') }}">Logout <b>{{ app.user.username }}</b></a>
            {% else %}
                <a class="myButton" href="{{ path('login') }}">Login</a>
            {% endif %}
        </header>
        {{ include('EventBundle:Event:_events.html.twig') }}
    </section>
{% endblock %}
